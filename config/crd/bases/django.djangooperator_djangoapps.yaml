---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: djangoapps.django.djangooperator
spec:
  group: django.djangooperator
  names:
    kind: DjangoApp
    listKind: DjangoAppList
    plural: djangoapps
    singular: djangoapp
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DjangoApp is the Schema for the djangoapps API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DjangoAppSpec defines the desired state of DjangoApp.
            properties:
              values:
                x-kubernetes-preserve-unknown-fields: true
            type: object
          status:
            description: DjangoAppStatus defines the observed state of DjangoApp.
            properties:
              created:
                description: Created is when the chart was first installed.
                format: date-time
                type: string
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
